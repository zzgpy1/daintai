apply plugin: 'com.android.application'

android {
    namespace "com.globalradio.app"
    compileSdk rootProject.ext.compileSdkVersion

    defaultConfig {
        applicationId "com.globalradio.app"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "2.0.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    signingConfigs {
        // 如果已有正式密钥，请替换以下路径和密码；如果没有，请参考下方生成方法
        release {
            storeFile file("your-keystore.jks")          // 密钥库文件路径（放在 app/ 目录下）
            storePassword "your-store-password"
            keyAlias "your-key-alias"
            keyPassword "your-key-password"
            // 启用 v1 和 v2 签名（解决安装问题）
            v1SigningEnabled true
            v2SigningEnabled true
        }
        debug {
            // 调试签名使用默认 debug keystore（不填也可）
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release  // 使用 release 签名
        }
        debug {
            // 可选择性使用 debug 签名，一般不需要指定
        }
    }
}

repositories {
    flatDir {
        dirs '../capacitor-cordova-android-plugins/src/main/libs', 'libs'
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation project(':capacitor-android')
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"

    // 添加 Capacitor 插件依赖（自动生成，无需手动添加）
    implementation project(':capacitor-cordova-android-plugins')
}

// 以下内容由 Capacitor 自动生成，保持原样
apply from: 'capacitor.build.gradle'

// 解决 google 服务问题（如有）
// apply plugin: 'com.google.gms.google-services'
