# 使用官方公共源
registry=https://registry.npmjs.org/
# 或使用国内镜像（二选一）
# registry=https://registry.npmmirror.com

# 确保没有私有源或认证 token 配置
# 例如，移除类似下面这样的行：
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}
# @my-scope:registry=https://private-registry.com/
